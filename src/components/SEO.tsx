// Stub: SEO é via Next.js metadata API. Componente fica vazio para compatibilidade
// com código portado do Lovable (que tinha <SEO ... /> em runtime).
export default function SEO(_props: Record<string, unknown>) {
  return null;
}
